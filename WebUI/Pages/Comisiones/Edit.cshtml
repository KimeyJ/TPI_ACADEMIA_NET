@page
@model WebUI.Pages.Comisiones.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Comision</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Comision.ComisionId" />
            <div class="form-group">
                <label asp-for="Comision.IdPlan" class="control-label"></label>
                <select asp-for="Comision.IdPlan" class="form-control" asp-items="ViewBag.IdPlan"></select>
                <span asp-validation-for="Comision.IdPlan" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Comision.Descripcion" class="control-label"></label>
                <input asp-for="Comision.Descripcion" class="form-control" />
                <span asp-validation-for="Comision.Descripcion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
