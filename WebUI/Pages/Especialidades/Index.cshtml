@page
@model WebUI.Pages.Especialidades.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form asp-page="./Index" method="get">
    <div class="form-actions no-color">
        <p>
            Nombre:
            <input type="text" name="filter" value="@Model.filterString" />
            <input type="submit" value="Buscar" class="btn btn-primary" /> |
            <a asp-page="./Index">Ver lista Completa</a>
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Especialidad[0].Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Especialidad) {
            @if (Model.filterString == null || item.Descripcion.Contains(Model.filterString))
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@item.EspecialidadId">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.EspecialidadId">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.EspecialidadId">Delete</a> |
                        <a asp-page="../Planes/Index" asp-route-id="@item.EspecialidadId">Planes</a>
                    </td>
                </tr>
            }   
}
    </tbody>
</table>
